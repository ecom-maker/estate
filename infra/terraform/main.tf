# Placeholder Terraform root
# Do not apply without explicit production approval.

terraform {
  required_version = ">= 1.5.0"
}

# Resources (documented only):
# - Vercel project
# - Supabase project reference
# - Redis (Upstash / Elasticache)
# - Worker compute
