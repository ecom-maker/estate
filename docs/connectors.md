# Connectors

External systems are isolated behind adapters.

```text
External API → Adapter → Normalizer → Domain model → Database
```

## Types

CRM · MLS · Transaction · Community · School · Metro · Airport

## Local mocks

- `GET /api/mock/crm`
- `GET /api/mock/mls`
- `GET /api/mock/transactions`

Replace mock adapters when real API documentation arrives without changing core property models.
